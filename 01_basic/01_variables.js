// VARIABLES: Una variable es un contenedor de información.

// Tipos de variables:

var nombre; // esta es una declaración
nombre = "Kevin"; // Esta es una asignación.

let apellido = "Gómez"; // let se utiliza para almacenar datos variables, es decir que pueden ser cambiados a futuro.

const ID = 10093; // Const se utiliza para guardar un valor inmutable, es decir no podra ser cambiado NUNCA!

// Las variables como ya informamos, guardan datos, estos datos son:

/*
    TIPOS DE DATOS 
    
    - String (Texto) => el string usa " " para guardar sus datos. 
    - int(número entero) BigInt => todos los numeros se guardan tal cual, sin comillas ni signos.
    - boolean(verdadero/falso) valores[true or false] -> tambien pueden contener condiciones. 
    - float(número decimal) double =>todos los numeros se guardan tal cual, sin comillas ni signos. (float suele usar f al final de sus valores) ejemplo let float = 145.8f.
    - null//NaN//Undefine//"" => VACIO (no contiene un valor)
*/

let mayorDeEdad = false; //  esto es un booleano

let licencia; // esta variable esta vacia (null//undefine...)

let altura = 1.67; // esta varible contiene un double.

console.log("Esto muestra por consola lo que este dentro de los parentesis")








